# UFNS environment template — copy to .env and fill in. Never commit .env.
# No secrets are required for the deterministic demo profile.

# --- Demo / data ---
UFNS_DATA_DIR=./data
UFNS_PILOT_ID=synthetic_wb_fixture_v1

# --- Application ---
UFNS_API_HOST=0.0.0.0
UFNS_API_PORT=8000
UFNS_LOG_LEVEL=INFO
UFNS_MAX_CONCURRENT_RUNS=1
UFNS_RUN_TIMEOUT_SECONDS=1800

# --- Live data (M10; only used after access is verified) ---
# IMD_API_KEY=
# MOSDAC_USER=
# MOSDAC_PASS=
